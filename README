
# sqlite-benchmark
benchmark the performance of sqlite

# scenarios
- discrete
- compound
- async
- ractors
- multiple tables

## discrete
- writes only to a single table
- reads only to a single table

## compound
- simple single process
- simultaneous reads and writes to a single table
- simultaneous reads and writes to seperate tables

## async
- async processing
- simultaneous reads and writes to a single table
- simultaneous reads and writes to seperate tables

